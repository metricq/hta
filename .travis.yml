language: generic

dist: xenial

matrix:
  include:
    - os: linux
      env: CXX="g++-7" CC="gcc-7"
      compiler: gcc
      addons:
        apt:
          packages:
            - gcc-7
            - g++-7
            - cmake
            - libboost-program-options-dev
            - libboost-system-dev
          sources: &sources
            - ubuntu-toolchain-r-test
    - os: linux
      env: CXX="g++-8" CC="gcc-8"
      compiler: gcc
      addons:
        apt:
          packages:
            - gcc-8
            - g++-8
            - cmake
            - libboost-program-options-dev
            - libboost-system-dev
          sources: *sources
script:
  - cmake --version
  - cmake .
  - make -j 2
  - ctest .
